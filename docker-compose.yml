version: '2'
services:
   mydbapp:
      build: ./web
      ports:
        - "8888:8080"
      depends_on:
        - db

   db:
      build: ./db
      volumes:
        - db_data:/var/lib/postgresql/data
      container_name: db

volumes:
   db_data:
